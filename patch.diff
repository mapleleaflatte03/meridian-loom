--- crates/loom-shadow/src/lib.rs
+++ crates/loom-shadow/src/lib.rs
@@ -1061,6 +1061,16 @@
     }
 }

+struct EventIds {
+    job_id: String,
+    execution_id: String,
+    decision_id: String,
+    parity_id: String,
+    audit_id: String,
+    subject_id: String,
+    source_event_id: String,
+}
+
 fn runtime_event_for_capture(capture: &RuntimeExecutionCapture) -> RuntimeEventV1 {
     let job_id = canonical_job_id(
         &capture.org_id,
